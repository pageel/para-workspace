# PARA Workspace Repo gitignore
# This repo contains ONLY governance (kernel, CLI, templates, workflows, docs)
# Runtime workspace dirs are NOT part of this repo

# System & IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log

# Dependencies
node_modules/
dist/
build/
coverage/

# Environment Variables
.env
.env.*
!.env.example

# Secrets (never commit)
_secrets/

# Agent runtime (workspace-only)
.agent/memory/
.agent/history/

# Workspace runtime dirs (should NOT exist in repo)
# These are generated by `para init` in workspaces only
Projects/
Areas/
Archive/
_inbox/

# Workspace metadata (workspace-only)
.para-workspace.yml
