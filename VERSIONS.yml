# VERSIONS.yml — Single Source of Truth for governed library item versions
#
# Convention:
#   version = workspace VERSION at time of last item content change.
#   ⛔ Do NOT bulk-update when bumping workspace version.
#   Only update entries for items that ACTUALLY changed.
#
# See: naming.md §6 (date format), versioning.md §5 (version tracking)
# Updated: 2026-05-22

workspace: "1.8.10"

workflows:
  backlog:         "1.8.10"  # updated: graph router integration
  open:            "1.8.10"  # Project Skill + memory_search at session start
  end:             "1.8.10"  # Project Skill + auto memory_curate
  install:         "1.0.0"
  new-project:     "1.8.10"  # updated: next action suggestion hook
  verify:          "1.8.9"   # graph_impact_analysis blast radius check
  retro:           "1.8.9"   # memory_search + memory_push + memory_curate
  push:            "1.0.1"
  release:         "1.0.1"
  merge:           "1.0.0"
  config:          "1.7.13"   # updated: preferences.date_format support
  backup:          "1.0.0"
  para:            "1.1.0"
  inbox:           "1.1.0"
  learn:           "1.8.9"   # memory_push for lessons
  para-workflow:   "1.8.8"   # Purged legacy catalog.yml version references
  para-rule:       "1.8.8"   # Purged legacy catalog.yml version references
  plan:            "1.8.10"  # Project Skill + memory_search + graph_edges
  para-audit:      "1.8.8"   # Quarantine Hook
  docs:            "1.8.10"  # Modular HTML Renderer + watch option
  update:          "1.8.5"   # BUG-34: consumer fetch+reset for Windows
  brainstorm:      "1.8.10"  # Project Skill + memory lifecycle
  para-knowledge:  "1.4.0"
  para-skill:      "1.8.8"   # Purged legacy catalog.yml version references
  write:           "1.7.16"   # FEAT-79: style guide, social template
  logs:            "1.8.9"   # memory_search cross-reference in deep audit
  spec:            "1.8.10"  # --graph pipeline + Project Skill + memory ops
  qa:              "1.8.10"  # memory_search + memory_push + memory_curate

rules:
  agent-behavior:          "1.7.16"   # C7 file guards, proactive guard scan
  governance:              "1.0.0"
  artifact-standard:       "1.0.0"
  context-rules:           "1.1.0"
  exports-data:            "1.0.0"
  naming:                  "1.7.13"   # updated: date format convention
  para-discipline:         "1.0.0"
  vcs:                     "1.7.13.2" # updated: SafeToAutoRun gate
  versioning:              "1.7.13"   # updated: VERSIONS.yml reference
  hybrid-3-file-integrity: "1.7.16"   # C7: plan status transition is USER-ONLY
  knowledge:               "1.1.0"
  agent-persona:           "1.8.10"   # NEW: Custom conversational style
  graph-first-policy:      "1.8.10"   # NEW: Graph-First Investigation Policy
  tool-routing:            "1.8.10"   # NEW: Tool selection heuristic guide

skills:
  para-kit:    "1.8.8"   # Purged legacy catalog.yml version references
  formatting:  "1.1.0"
  page-map:    "1.0.0"
  para-skill:  "1.0.0"
  plan:        "1.8.10"  # Graph Impact added to templates
  docs:        "1.8.10"  # graph router §3.3.2 Graph-Awareness
  brainstorm:  "1.8.10"  # graph router §3.3.3 Graph-Awareness
  write:       "1.7.16"   # social template added
  harness:     "1.8.7"   # Enforced governance guards
  spec:        "1.8.10"  # graph router §3.3.4 Graph-Awareness
  qa:          "1.8.10"  # Force Red Team @[/brainstorm] for Critical
  tdd:         "1.8.8"   # Automated TDD Gate (--gate)
  logs:        "1.8.7"   # NEW: Logs Audit Extensions
  html-renderer: "1.8.10" # NEW: Modular HTML Renderer skill
  new-project: "1.8.10"  # NEW: Sidecar skill for /new-project
  para-graph:  "1.8.10"  # NEW: Centralized Graph Intelligence Router

cli:
  para:            "1.8.2"   # MCP Management commands, help text
  install-tool:    "1.8.10"  # hook detection + --sync flag + semver_gte
  remove-tool:     "1.8.0"   # NEW: remove installed tools
  list-tools:      "1.8.0"   # NEW: list installed tools
  install:         "1.8.0"   # Add tool system asset sync (section 3.6)
  mcp-setup:       "1.8.2"   # NEW: manifest-declared MCP auto-config
  mcp-list:        "1.8.2"   # NEW: list MCP-capable tools
  mcp-remove:      "1.8.2"   # NEW: remove IDE MCP config

templates:
  tool-wrapper:    "1.8.0"   # NEW: auto-generated wrapper for tool plugins

registry:
  tools:           "1.8.5"   # para-graph latest bumped to 0.8.6
  tool-schema:     "1.8.5"   # shipped_in + min_engine_version (additive)

knowledge:
  para_workspace_architecture_standards: "1.8.10" # TDD Governance Hygiene & QA Red Team Upgrade
  para_anti_truncation_guardrail: "1.8.10" # NEW: Anti-truncation mechanism and safety guardrails

