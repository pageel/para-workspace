# VERSIONS.yml — Single Source of Truth for governed library item versions
#
# Convention:
#   version = workspace VERSION at time of last item content change.
#   ⛔ Do NOT bulk-update when bumping workspace version.
#   Only update entries for items that ACTUALLY changed.
#
# See: naming.md §6 (date format), versioning.md §5 (version tracking)
# Updated: 2026-04-24

workspace: "1.8.2"

workflows:
  backlog:         "1.3.1"
  open:            "1.7.13.2" # updated: file link constraint
  end:             "1.7.16"   # FEAT-71: remove auto-create KI suggestion
  install:         "1.0.0"
  new-project:     "1.1.0"
  verify:          "1.0.0"
  retro:           "1.7.1"
  push:            "1.0.1"
  release:         "1.0.1"
  merge:           "1.0.0"
  config:          "1.7.13"   # updated: preferences.date_format support
  backup:          "1.0.0"
  para:            "1.1.0"
  inbox:           "1.1.0"
  learn:           "1.7.1"
  para-workflow:   "1.7.14"  # updated: BUG-32 fix source: user default
  para-rule:       "1.0.0"
  plan:            "1.7.15"   # status gate, detail-plan refactor
  para-audit:      "1.7.1"
  docs:            "1.7.10"
  update:          "1.0.1"
  brainstorm:      "1.7.12"
  para-knowledge:  "1.4.0"
  para-skill:      "1.0.1"
  write:           "1.7.16"   # FEAT-79: style guide, social template
  logs:            "1.7.15"   # FEAT-78: command granularity + KI token budget
  spec:            "1.7.16"   # NEW: specification writer workflow

rules:
  agent-behavior:          "1.7.16"   # C7 file guards, proactive guard scan
  governance:              "1.0.0"
  artifact-standard:       "1.0.0"
  context-rules:           "1.1.0"
  exports-data:            "1.0.0"
  naming:                  "1.7.13"   # updated: date format convention
  para-discipline:         "1.0.0"
  vcs:                     "1.7.13.2" # updated: SafeToAutoRun gate
  versioning:              "1.7.13"   # updated: VERSIONS.yml reference
  hybrid-3-file-integrity: "1.7.16"   # C7: plan status transition is USER-ONLY
  knowledge:               "1.1.0"

skills:
  para-kit:    "1.1.0"
  formatting:  "1.1.0"
  page-map:    "1.0.0"
  para-skill:  "1.0.0"
  plan:        "1.8.1"   # execution ownership icons, graph-first conditional step
  docs:        "1.7.15"   # strategy reference roadmap prefix
  brainstorm:  "1.7.16"   # FEAT-83: decision template linear text upgrade
  write:       "1.7.16"   # social template added
  harness:     "1.7.16"   # dual-format convention, checkpoint type, commit consolidation
  spec:        "1.7.16"   # NEW: spec-driven-development skill

cli:
  para:            "1.8.2"   # MCP Management commands, help text
  install-tool:    "1.8.2"   # MCP detect block, --no-mcp flag
  remove-tool:     "1.8.0"   # NEW: remove installed tools
  list-tools:      "1.8.0"   # NEW: list installed tools
  install:         "1.8.0"   # Add tool system asset sync (section 3.6)
  mcp-setup:       "1.8.2"   # NEW: manifest-declared MCP auto-config
  mcp-list:        "1.8.2"   # NEW: list MCP-capable tools
  mcp-remove:      "1.8.2"   # NEW: remove IDE MCP config

templates:
  tool-wrapper:    "1.8.0"   # NEW: auto-generated wrapper for tool plugins

registry:
  tools:           "1.8.0"   # NEW: tool registry (para-graph first entry)
  tool-schema:     "1.8.2"   # mcp: property support

knowledge:
  para_workspace_architecture_standards: "1.8.2" # added mcp auto-setup features
