# v1.7.4 — Repo Path Standardization, Pending TODO Fix & Project Profile Skill

> **Released:** 2026-04-03

## Bug Fixes

### BUG-28 Followup: Repo Template Path Rename

- **Renamed** `templates/common/agent/` → `templates/common/agents/` to match workspace convention (`.agents/`)
- **Updated** 50+ file references across repo, internal docs, project rules, and plan files
- **Fixed** `install.sh` and `migrate.sh` hardcoded paths (`LIB_SRC`)
- **Fixed** `validator.sh` usage comment
- **Excluded** 4 changelog files from batch sed (immutable history)
- **Updated** System KI metadata paths in `para_workspace_architecture_standards`

### BUG-29: Pending TODO Redundancy

- **Removed** `⏳ Pending TODO` section from `/open` Step 8 report template
- **Removed** `Pending TODOs (carry-forward items)` from `/end` Step 2 session log template
- **Updated** brainstorm carry-forward text to reference Hot Lane (`sprint-current.md`) instead of pending TODO
- **Rationale:** Session logs are immutable journals (I2). Task tracking belongs exclusively in the Hybrid 3-File model (backlog.md, sprint-current.md, done.md)

## New Features

### FEAT-62: Project Profile Skill

- **Created** `.agents/skills/project-profile/SKILL.md` — project-specific conventions that influence workspace-level workflows
- **Sections:** §1 Identity, §2 Naming, §3 Maintenance, §4 Quality, §5 Docs Flow, §6 Plan Review Checklist
- **Plan Review Checklist (§6):** Automated quality gate for `/plan create` and `/plan update` — catches logic errors, security gaps, missing scope, and token bloat before execution
- **Token decay defense:** §6 includes re-read instruction to counter attention decay in long planning sessions
- **Created** generic template at `templates/project/agents/skills/project-profile/SKILL.md`
- **Created** project skills index `.agents/skills.md` with trigger table
- **Enabled** `agent.skills: true` in `project.md`

## Technical Notes

- `migrate.sh` backward compatibility preserved — Step 12 still detects old `.agent/` path for workspace migration
- Badge URLs verified manually per §14 lesson
- System KI `para_version` bump included for sync gate
