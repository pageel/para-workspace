# v1.6.2 — Unified Agent Index

> **Released:** 2026-03-24 | **Type:** MINOR feature | **FEAT-53**

## Summary

Introduces the **Unified Agent Index** — a parallel skills trigger index alongside the existing rules index, a new `agent` map in project schema replacing `has_rules`, and a **Proactive Trigger Check** mechanism that ensures agents scan all trigger tables before performing side-effect actions.

## What's New

### Skills Trigger Index

| Item | Path |
| :--- | :--- |
| Template | `repo/templates/common/agent/skills.md` |
| Workspace copy | `.agents/skills.md` |
| Synced by | `install.sh` (mirrors `rules.md` logic) |

The skills index follows the same Two-Tier Progressive Disclosure pattern as rules — agent reads the index (~10 lines) at session start and loads individual skills on demand when triggers match.

### Agent Map in project.md

```yaml
# v1.6.2+ (new)
agent:
  rules: true
  skills: true

# Legacy (deprecated, backward compat kept)
has_rules: true
```

The `/open` workflow checks `agent.rules` first, falls back to `has_rules` for backward compatibility.

### Proactive Trigger Check

Before ANY action that edits files, runs commands, or creates artifacts:

1. Scan workspace `rules.md` trigger table
2. Scan workspace `skills.md` trigger table
3. Scan project trigger tables (if loaded)
4. **IF match found → read the rule/skill file BEFORE acting**

> Principle: Check THEN act — never act THEN check.

### Agent Indices Pre-flight (Layer 3)

All 7 workflows with Step 0 now re-read both `rules.md` AND `skills.md`:

- `/plan`, `/docs`, `/backlog`, `/push`, `/release`, `/retro`, `/end`

This guards against attention decay in long conversations — even if `/open` loaded indices at session start.

## Templates Changed

| Category | File | Change |
| :--- | :--- | :--- |
| **New** | `templates/common/agent/skills.md` | Skills trigger index template |
| **Schema** | `kernel/schema/project.schema.json` | `agent` map added, `has_rules` deprecated |
| **Workflow** | `workflows/open.md` v1.5.0 | Step 2.5a/b/c (rules + skills + project) |
| **Workflow** | `workflows/plan.md` v1.5.0 | Step 0 Pre-flight, D3 skills check |
| **Workflow** | `workflows/para-audit.md` v1.3.0 | Agent Index Consistency check |
| **Workflow** | `workflows/docs.md` v1.1.1 | Step 0 Pre-flight |
| **Workflow** | `workflows/backlog.md` v1.3.1 | Step 0 Pre-flight |
| **Workflow** | `workflows/end.md` v1.5.1 | Step 0 Pre-flight |
| **Workflow** | `workflows/push.md` v1.0.1 | Step 0 Pre-flight |
| **Workflow** | `workflows/release.md` v1.0.1 | Step 0 Pre-flight |
| **Workflow** | `workflows/retro.md` v1.0.1 | Step 0 Pre-flight |
| **Rule** | `rules/context-rules.md` v1.1.0 | §1 + §4 updated |
| **Rule** | `rules/agent-behavior.md` v1.1.0 | §4 updated |
| **Index** | `templates/common/agent/rules.md` | Proactive scan warning |
| **Template** | `templates/common/project.md` | `has_rules` deprecated note |
| **CLI** | `cli/commands/install.sh` | `skills.md` sync |

## Catalogs Bumped

- `workflows/catalog.yml`: 9 entries updated
- `rules/catalog.yml`: 2 entries updated
- `skills/catalog.yml`: `kernel_min` → 1.6.2

## Migration

No migration needed. Backward compatible:
- `has_rules: true` continues to work (fallback in `/open`)
- Projects without `agent` map are unaffected
- `/para-audit update` will suggest migration from `has_rules` to `agent` map

## Defense-in-Depth Update

```
Layer   What                         Where                           Survives
─────   ─────────────────────────    ─────────────────────────────   ────────
  1     Index instructions           agent-behavior.md §4            ⚠️ Lost after truncation
  2     Safety block in output       /open Step 8 report             ✅ In checkpoint summary
  3     Workflow pre-flight          Step 0: re-read rules + skills  ✅ Fresh from disk
  4     File guard headers           <!-- ⚠️ ... --> in file         ✅ Inline in target file
```

Layer 3 now includes **Proactive Trigger Check** — the "Check THEN act" principle.
